# Maser Lab

Experimental and foundational projects for Maser Media live under `maser-lab/`.

## Projects

| Path | Description |
| --- | --- |
| `maser-lab/surface-engine` | **Maser Surface Engine** — procedural monochrome material renderer |

Each lab project is an independent Next.js app with its own `package.json`.

```bash
npm run lab:surface
```
